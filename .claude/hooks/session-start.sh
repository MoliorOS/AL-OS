#!/usr/bin/env bash
# AL-OS session start — read-only orientation, no side effects.
cat <<'MSG'
[AL-OS session start]
Read before working: IDENTITY.md, AGENTS.md (routing table), ROADMAP.md.
This repo is human-invoked, client-owned. AL-BRAIN is written only through gbrain — never
directly. See AGENTS.md Core Rules before making any change.
MSG
