---
name: brief-playback
version: 0.0.1
status: specified
description: >
  Structure the client-briefing session Oliver named as AL's "missing workflow" — ask a
  consistent set of questions, capture the answers in a table, and play them back to the client
  for confirmation. The confirmed table becomes the project's anchor brief, re-checked at every
  subsequent stage gate.
domain: delivery
module: M2
owner: Andrew Bonner / Oliver Lowrie
access-tier: practice
triggers:
  - "run the brief playback for [project]"
  - "start the client briefing session"
allowed-tools: TBD — not yet built
fixes: F08 (structured brief + playback not consistently held — root cause of the L1 brief-to-delivery gap)
---

Not yet built. Depends on `context/delivery.md`'s kickoff section (step 2 — currently named as
"the missing workflow" with no template or owner yet assigned) being turned into an actual
question set before this skill's procedure can be written.
